# Registries

Per [DECISIONS.md](../../docs/DECISIONS.md) (D-008, D-003):

- Bedrock: JSON from [Mojang/bedrock-protocol-docs](https://github.com/Mojang/bedrock-protocol-docs) per supported patch (2× 1.21.x).
- Java: derived mapping tables aligned to the same internal `BlockId` / `ItemId` keys.

Populate this directory when `mcrust-registry` crate is added.